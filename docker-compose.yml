version: '3.8'

services:
  backend:
    build: .
    container_name: imotarak-backend
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev
